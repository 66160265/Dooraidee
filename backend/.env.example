PORT=4000
TMDB_READ_ACCESS_TOKEN=
MAL_CLIENT_ID=
# Optional: restrict CORS to your deployed frontend's origin(s) in production
# (comma-separated). Leave unset to allow all origins (fine for local dev).
FRONTEND_URL=
